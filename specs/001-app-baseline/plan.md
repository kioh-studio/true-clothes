# Implementation Plan: True Clothes App — Current State Baseline

**Branch**: `001-app-baseline` | **Date**: 2026-06-06 | **Spec**: specs/001-app-baseline/spec.md

**Input**: Feature specification from `specs/001-app-baseline/spec.md`

## Summary

True Clothes has a well-built UI layer and a sophisticated server-side outfit generation
engine, but personalisation is broken end-to-end: user wardrobe items are stored only
in local device storage and never synced to Supabase. The fit engine's Edge Function
reads from an empty database and falls back to pre-seeded demo items — real personalised
outfit generation does not work.

This plan addresses that critical gap, closes remaining stubbed UI features (share,
settings, help & feedback), adds live weather integration to the feed, and establishes
the SQL migration infrastructure the project currently lacks.

**Three categories of work:**

1. **Fix** — Wardrobe sync to Supabase (unblocks end-to-end personalisation) and SQL
   migration files (establishes schema version control)
2. **Complete** — Share outfit, Settings screen, Help & Feedback, feed error recovery UI
3. **Extend** — Live weather API integration via Open-Meteo (no API key required)

---

## Technical Context

**Language/Version**: TypeScript 5.x (strict mode) · React Native 0.81.5 · React 19

**Primary Dependencies**:
- Expo SDK: Router, ImagePicker, Location, Font, FileSystem
- Zustand 5.0.13 with AsyncStorage persist middleware
- @supabase/supabase-js: auth, database, storage, edge functions
- React Navigation: stack + tab navigators (via Expo Router)

**Storage**:
- Local: AsyncStorage (Zustand persist) — session state, UI toggle state
- Remote DB: Supabase PostgreSQL — profiles, measurements, style profiles, clothing items
- Remote Files: Supabase Storage (`wardrobe-photos` bucket) — clothing item photos

**Testing**: Jest + ts-jest · `src/**/__tests__/`

**Target Platform**: iOS 15+ and Android 12+ via Expo managed workflow

**Project Type**: Mobile app (Expo Router, file-based navigation)

**Performance Goals**:
- Outfit feed scroll: 60 fps
- Edge Function response: < 3 s on warm instance
- App cold start to interactive: < 3 s on mid-range device
- Wardrobe photo upload: < 5 s per photo on 4G

**Constraints**:
- Offline-capable for saved/worn/scheduled/local wardrobe state
- Network required for outfit generation and wardrobe sync
- No API keys committed to source control; `EXPO_PUBLIC_*` env vars only
- Supabase client never imported directly from screens, components, or stores

**Scale/Scope**: Personal-use app. ~50 screens, ~15 k LOC. Wardrobe expected < 200
items per user. Single-region Supabase deployment.

---

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-checked after Phase 1 design.*

| Gate | Applies? | Result | Notes |
|------|----------|--------|-------|
| 1. New screen/component → design token compliance | Yes — `settings.tsx`, `help.tsx` | ✅ Pass | New screens use `src/design/tokens.ts` exclusively. No inline hex values. |
| 2. Business logic → store/hook, not JSX | Yes | ✅ Pass | `wardrobeService` and `weatherService` are pure service modules. Store actions call them. No async/await in JSX event handlers. |
| 3. Touches Supabase → through service module only | Yes — wardrobe CRUD + Storage | ✅ Pass | All Supabase calls contained in `src/services/wardrobeService.ts`. No direct client import in screens or stores. |
| 4. New entity → `src/types/`, metric units, named colors | Yes — `ClothingItem` remote row | ✅ Pass | `ClothingItem` already in `src/types/fitEngine.ts`. Remote snake_case row type stays inside `wardrobeService`. Colors stored as named strings. |
| 5. Scoring logic change → apply to both client + server | No | N/A | No changes to scoring algorithms in this phase. |
| 6. Multiple product domains → via stores, not direct imports | Yes — weather feeds into feed | ✅ Pass | Weather state lives in `appStore`. Feed hook reads from store only. `weatherService` never imported from screens. |

All gates pass. No Complexity Tracking entries required.

---

## Project Structure

### Documentation (this feature)

```text
specs/001-app-baseline/
├── plan.md                       ← this file
├── research.md                   ← Phase 0: decision log
├── data-model.md                 ← Phase 1: entity model + DB schema
├── quickstart.md                 ← Phase 1: end-to-end validation guide
├── contracts/
│   ├── wardrobe-service.md       ← wardrobeService TypeScript contract
│   └── weather-service.md        ← weatherService TypeScript contract
└── tasks.md                      ← Phase 2 (generated by /speckit-tasks)
```

### Source Code (Expo Router — mobile structure)

```text
app/
├── _layout.tsx                   # (existing) add weather hydration on boot
├── (onboarding)/                 # (existing) no changes
├── (tabs)/
│   ├── index.tsx                 # (existing) wire weather context to feed filter
│   ├── wardrobe.tsx              # (existing) wire sync-status indicator
│   ├── menu.tsx                  # (existing) add navigation to settings + help
│   └── profile.tsx               # (existing) no changes
├── settings.tsx                  # NEW: Settings screen
└── help.tsx                      # NEW: Help & Feedback screen

src/
├── services/
│   ├── supabase.ts               # (existing) no changes
│   ├── authService.ts            # (existing) no changes
│   ├── profileService.ts         # (existing) no changes
│   ├── measurementService.ts     # (existing) no changes
│   ├── styleProfileService.ts    # (existing) no changes
│   ├── wardrobeService.ts        # NEW: clothing_items CRUD + Storage photo upload
│   └── weatherService.ts         # NEW: Open-Meteo current weather fetch + cache
├── stores/
│   ├── authStore.ts              # (existing) no changes
│   ├── appStore.ts               # UPDATE: integrate wardrobeService + weatherService
│   └── fitEngineStore.ts         # (existing) no changes — already calls Edge Function
├── types/
│   └── fitEngine.ts              # (existing) ClothingItem already defined
└── design/
    └── wardrobe/
        └── design.md             # UPDATE: sync-status visual treatment

supabase/
├── migrations/
│   ├── 20260606000001_initial_schema.sql    # NEW: profiles, body_measurements, style_profiles
│   ├── 20260606000002_clothing_items.sql    # NEW: clothing_items table + RLS policies
│   └── 20260606000003_storage_policies.sql  # NEW: wardrobe-photos bucket access policies
└── functions/
    └── generate-outfits/         # (existing) no changes required
```

**Structure Decision**: Expo Router mobile structure. New screens added to `app/` root
alongside existing modal screens. All backend access goes through the established
`src/services/` abstraction layer. SQL migrations introduced to `supabase/migrations/`
for the first time, establishing version-controlled schema management.

---

## Complexity Tracking

> No Constitution Check violations. This section intentionally empty.
