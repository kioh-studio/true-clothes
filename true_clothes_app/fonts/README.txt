Fonts folder
------------
Poppins and Playfair Display are **not** bundled here. The app uses the **google_fonts** package, which downloads the same families from **Google Fonts** at runtime and caches them on the device (first launch needs network on Android — see INTERNET in AndroidManifest).

No `.ttf` files are required in this folder for the current setup.
