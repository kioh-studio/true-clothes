
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Local secrets — never commit
.env
.env.local

# Dependencies
node_modules/

# Expo
.expo/
dist/

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Build artifacts
*.log
*.orig

# Large binaries / archives
*.zip

# Crash dumps (e.g. from wedged shells)
*.stackdump

# Scratch design exports / temp screens (kept locally as visual reference only)
screens/*-temp/

# Local native-module build outputs (CNG/prebuild) — not covered by android/.gitignore
modules/*/android/build/

# Gradle caches / daemon state — regenerated on every build, never source
.gradle/

# Android debug keystore — regenerated by prebuild; keep keys out of VCS
android/app/debug.keystore

# Claude Code local workspace (settings, skills, commands) — local only
.claude/
